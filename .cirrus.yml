env:

    # Cirrus Specific Vars (Do *NOT* Change)
    CIRRUS_SHELL: bash

task:
  name: "Recovery Build"
  timeout_in: 120m
  container:
      image: ghcr.io/sushrut1101/docker:arch
      cpu: 8
      memory: 40G

  clone_script:
      - curl -sL https://raw.githubusercontent.com/Nico170420/CI-Scripts/shrp/clone.sh | bash

  building_script:
      - curl -sL https://raw.githubusercontent.com/Nico170420/CI-Scripts/shrp/building.sh | bash

  Upload_script:
      - curl -sL https://raw.githubusercontent.com/Nico170420/CI-Scripts/shrp/upload.sh | bash

  Upload2_script:
      - curl -sL https://raw.githubusercontent.com/Nico170420/CI-Scripts/shrp/upload2.sh | bash